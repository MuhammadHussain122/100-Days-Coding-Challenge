/* Write a function min(a,b) which returns the least of two numbers a and b.

For instance:

min(2, 5) == 2
min(3, -1) == -1
min(1, 1) == 1
*/

function min(num1, num2){
    return Math.min(num1, num2);
}
console.log(`Least of the two numbers : ${min(3,6)}`);
