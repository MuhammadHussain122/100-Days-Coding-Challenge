/* Determine the Remainder:
Make a function that gets two numbers and shows the leftover by dividing them using the % sign.
*/

function Remainder(Dividend: number, Divisor: number){
    return Dividend % Divisor;
}

console.log(Remainder(41, 3));
