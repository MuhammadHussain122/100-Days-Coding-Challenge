/* Converting Strings to Numbers:
Create a function that takes a string representing a number (like "123") and converts it into an actual number tree.
*/

function stringRepresentingNumber(str: string){
    return Number(str);
} 
console.log(stringRepresentingNumber("64.6"));
console.log(stringRepresentingNumber("68"));