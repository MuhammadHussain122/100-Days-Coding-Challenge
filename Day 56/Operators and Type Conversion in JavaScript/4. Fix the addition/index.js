/* Here’s a code that asks the user for two numbers and shows their sum.

It works incorrectly. The output in the example below is 46 (for default prompt values).

Why? Fix it. The result should be 10.


let a = prompt("First number?", 4);
let b = prompt("Second number?", 6);

alert(a + b); // 46
*/

// Fixation
let a = 4;
let b = 6;
let c = prompt("First number?", a);
let d = prompt("Second number?", b);

alert(a + b); // 10

