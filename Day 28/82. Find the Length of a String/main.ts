/* Find the length of a String:
Write a function that takes a string as an argument and returns the number of characters in the string.
*/

function stringAsArgument(String: string): number{
    return String.length;
    
}
console.log(`The number of characters in the string "Hussain" is ${stringAsArgument("Hussain")}`);
