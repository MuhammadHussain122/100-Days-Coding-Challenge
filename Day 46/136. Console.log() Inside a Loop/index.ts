/*
Use console.log() to debug and track the value of a variable inside a loop.
*/
import chalk from "chalk";
for (let counter = 0; counter <= 4; counter++) {
  console.log(chalk.green(`counter ${counter}, counter: value`));
}
