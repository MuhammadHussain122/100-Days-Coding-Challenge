/* Extracting a Substring:
Demonstrate how to extract the first 10 characters from a string.
*/
function extractingSubString(str) {
    return str.substring(0, 9);
}
console.log(extractingSubString("Knowledge is power"));
export {};
