/* Create an arrow function that takes multiple parameters and returns the product of all parameters.
*/

let product = (num1: number, num2: number, num3: number) => {
    return num1* num2 * num3;
}
console.log(product(2, 3, 5));